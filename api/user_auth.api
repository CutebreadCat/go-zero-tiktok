syntax = "v1"

import "user.api"

@server(
  group: user
  jwt: Auth
  tags:"user"
)
service tiktok-api {
  @handler PostUserPhotoHandler
  put /user/avatar/upload (UserphotoRequest) returns (UserphotoResponse)
}
