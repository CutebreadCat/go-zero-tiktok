syntax = "v1"

import "./video.api"

@server(
  group: video
  jwt: Auth
  middleware: RateLimit
  tags:"video"
)
service tiktok-api {
  @handler PublishVideoHandler
  post /video/publish (PublishVideoRequest) returns (PublishVideoResponse)
}
