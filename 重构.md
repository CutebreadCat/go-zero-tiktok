重构微服务技术文档
1.不要动api层,然后每个模块新增rpc层
2.rpc层采用go_zero的生成工具去生成 补上-style go_zero
3.然后的话可以参照我的的这个用户模块去进行一个迁移,但是不要再将数据库之类的外部依赖注入到svc之中,采用服务内聚和依赖注入的方式
4.然后的话我的这个internal层后续会下沉成一个通用工具层
5.主要是去做代码的迁移,不要去动具体实现
6.我已经做好具体的目录和领域的划分,你按照这个划分去做就行,在这个app目录下面
7.一定要解耦,采用依赖注入的方式,logic当中不能直接去引用dal这种,然后对应的迁移方式我的用户模块
8.各个模块的svc那边也实现一个高内聚的模型,不然的话这样太分散了,我的internal不好动,我的internal现在还需要去做一个网关层
9.修改我的svc模型,不要再直接注入中间件之类的模型,不然的话耦合性太高了
10.然后具体的结构层次参照我的internal的domain层的一个划分,然后的话解耦思想采用具体的ddd设计驱动原则